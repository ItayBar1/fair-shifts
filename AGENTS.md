# Fair Shifts — הוראות עבודה לסוכנים

## מטרת המאגר והשלב הנוכחי

Fair Shifts היא מערכת מתוכננת לניהול וחלוקת תורנויות ביחידה של כ־120 חיילים. האתר בעברית, מימין לשמאל, עם ממשקי חיילים, אחראי תורנויות ומנהל טכני נפרד.

המאגר כולל כעת אפיון ומסמכי מסירה לתכנון. אין עדיין קוד יישום, סכמת מסד, סקריפטי הרצה או תשתית בדיקות יישום. אל תניח שהם קיימים ואל תדווח שבדיקות מערכת עברו כשנבדקו רק מסמכים. השלב הבא הוא תוכנית מימוש מתוך קובצי Git, ללא צורך בגישה לצ׳אט המקורי.

## התחלת סשן

1. קרא את הקובץ הזה ואת [מצב הפרויקט](config/memory/project-state.md). בדוק את מצב Git ואת השינויים הקיימים לפני עריכה; שמור עבודה שאינה שלך.
2. קרא את [מדריך המעבר לתכנון](docs/planning-handoff.md).
3. לתכנון מוצר או מימוש, קרא את [האפיון המלא](docs/duty-management-prd.md), ובדוק את [יומן ההכרעות](docs/open-decisions.md) ואת [מפת הכיסוי](docs/requirements-coverage.md) בנושאים הרלוונטיים.
4. עיין ב[מחקר](docs/research-notes.md) רק כשנוגעים בדרגות, בספק המייל או בגיבוי. הפרד עובדות חיצוניות מהחלטות מוצר.
5. קרא את הרשומה האחרונה ב[יומן הסשנים](config/memory/session-log.md), ובדוק את המציאות במאגר. הזיכרון מסייע להתמצאות ואינו הוכחה שקוד או בדיקה קיימים.

כל הנתיבים כאן יחסיים לשורש המאגר. אין תלות בתיקיית עבודה מקומית, בכלי של ספק מסוים או בקבצים שהיו במחשב שבו נערך הראיון.

## מקורות אמת ושינויים

- הוראה מפורשת ועדכנית של המשתמש קודמת למסמכים. תעד שינוי מוסכם באפיון וביומן ההכרעות, כדי שגם סשן הבא יכיר אותו.
- האפיון ב־Markdown הוא מקור דרישות המוצר. יומן ההכרעות מתעד משמעות והחלפות של החלטות; מפת הכיסוי מקשרת לדרישות ולמקרי הקבלה.
- אין להסיק מדיניות חדשה רק מתשובה קצרה ישנה. ביומן כבר נכתב פירוש ההחלטות המאושר, כולל הסעיפים שהוחלפו בהמשך.
- הזיכרון הוא סיכום תפעולי, לא מקום לשנות דרישות. אם הוא סותר את האפיון, בדוק היסטוריה ומקור ועדכן את הסיכום; אל תבחר בשקט מדיניות אחרת.
- שאלה חדשה נדרשת כשחסר כלל עסקי מהותי או יש סתירה שלא נפתרה. בחירות מימוש רגילות יש לפתור מתוך ההיקף הקיים ולתעד את הנימוק. אל תפתח מחדש הכרעות סגורות ללא סיבה.
- קובץ HTML הוא עותק תצוגה של ה־Markdown. עדכון באפיון מחייב לסנכרן גם אותו ואת הגרסה המוצגת; ההכרעה במקרה של אי־התאמה היא לפי ה־Markdown.

## כללים שאסור לאבד בתכנון

רשימה זו היא תזכורת בלבד; הנוסח המלא באפיון מחייב.

- טבלת צדק אחת. שלוש אוכלוסיות שיבוץ: חובה, קבע/קצינים וקמ״א. קצין בחובה שייך לקבע/קצינים; קמ״א אינו עובר לשם רק בגלל הכינוי קצין. שני אחראים יכולים לנהל את כולם; סינון ברירת מחדל אינו הרשאה.
- מסננים התאמה לפני חישוב המינימום. הניקוד לשיבוץ כולל ניקוד נוכחי ושיבוצים שטרם זוכו, גם בטיוטה ובביצוע. הרצועה היא `S(i) < min(S(E)) + w`, וההגרלה אחידה. אפס נקודות משובץ ידנית בלבד. מחשבים מחדש אחרי כל בחירה.
- דרגה נבדקת בתחילת התורנות; כשירות ואוכלוסייה לפי הכללים לכל המשך. פז״ם יוצר תזכורת לאחראי, לא קידום אוטומטי. עדכון דרגה דורש אישור או שינוי ידני עם תאריך תחולה.
- אילוץ מאושר חוסם. אילוץ ממתין מצריך אישור להמשיך לפני סקירה וגם אישור פרטני לכל התנגשות. חריגים לפטור ולדרגה הם ידניים, נקודתיים ומתועדים.
- פרסום מפורש; שינוי שפורסם דרך ״עדכן ופרסם״. השיבוץ המקורי נשאר עד להשלמת החלפה. החלפה בהסכמה אינה בודקת ניקוד, ומעבירה גם תוספת קיימת; אחרי התחלה נדרש אחראי.
- ניקוד שלם ורצפת אפס. תוספות מתחברות. זקיפה לפי מועד סיום הביצוע; תיקון שחוצה נרמול עשוי להמתין להכרעה ידנית. יש לשמר היסטוריה ותמונת ההגרלה בלי זיכוי כפול.
- מחיקת חשבון מסירה קשר ומידע רגיש, שומרת שם/מספר אישי והיסטוריית תורנויות/ניקוד, ומפנה שיבוצים עתידיים עם התראה. אי־פעילות, נעילה ושחרור הם מצבים נפרדים ממחיקה.
- מנהל טכני הוא חשבון נפרד. רק הוא מנהל הרשאות אחראים. קוד: 10 דקות, דקה בין שליחות, נעילה ידנית אחרי חמש טעויות עם אזהרות אחרי 3/4; חיבור חייל עד שבעה ימים ומנהלים עד 24 שעות.

## כיוון טכני וגבולות העבודה

הכיוון המאושר הוא TypeScript, Next.js לאתר ולשרת, עובד Node.js ו־PostgreSQL, ביישום אחד עם שמונה תחומים לוגיים. האירוח: Linux פרטי, Docker ו־Cloudflare Tunnel. המייל: Brevo Free עם ניהול מכסה. הגיבוי: Google Drive ייעודי, יומי ומוצפן, עד 30 עותקים בכפוף לנפח, ובדיקת שחזור רבעונית.

אין לבחור בשקט שירות בתשלום, אפליקציות נפרדות או ארכיטקטורה מבוזרת במקום ההיקף שסוכם. גרסאות ספריות, ORM, ספריית אימות, סכמת מסד ו־API עדיין שייכים לתכנון הטכני. בדוק תיעוד רשמי עדכני בעת בחירתם ותעד החלטות שישפיעו על המשך העבודה.

בעת יצירת תוכנית מימוש, חלק אותה לשלבים שאפשר לבדוק מקצה לקצה. קשר כל שלב לדרישות ולמקרי הקבלה. מימוש הדרגתי אינו אישור להסיר יכולות מהגרסה שנדרשה. אל תיצור משימות חיצוניות או תתחיל לפרוס מערכת רק כי מסמכי התכנון מוכנים.

## בדיקות ותיעוד

- האפיון כולל תרחישי קבלה מתוכננים. השתמש בהם לבדיקות התנהגות: כשירות, ניקוד, הרשאות, אילוצים, החלפות, מקביליות, ייבוא, מחיקה והודעות. בדוק גם גבולות זמן ישראל, עיגול ופעולות חוזרות.
- התאם בדיקות לשינוי; אל תוסיף בדיקות מלאכותיות לתיקון תיעוד בלבד. בשינוי מסמכים בדוק קישורים, מספור, התאמה בין ההחלטות והאפיון, וסנכרון HTML כשנדרש.
- כשיהיו פקודות build/test אמיתיות, תעד אותן ב־README. עד אז אין פקודת בדיקות יישום מאושרת ואין להמציא אחת בדוח.
- כתוב מסמכי מוצר והסברים למשתמש בעברית. קוד ומזהים יכולים להשתמש באנגלית לפי מוסכמות שייקבעו במימוש.

## זיכרון בין סשנים

[config/memory](config/memory/README.md) היא תיקייה מתועדת ב־Git. אין לה טעינה אוטומטית או מנגנון זיכרון נסתר; כל סוכן קורא ומעדכן את הקבצים לפי ההוראות כאן.

- `project-state.md`: תמונת מצב קצרה ומתעדכנת — מה קיים, מה הושלם, מה הבא, ותלויות או חסימות ממשיות.
- `session-log.md`: רשומות קצרות לפי תאריך עם השינוי, הקבצים, הבדיקות שבוצעו בפועל והמשך מומלץ. אין להעתיק אליו תמליל צ׳אט או פלטי כלים ארוכים.
- נושא מתמשך יכול לקבל קובץ ממוקד נוסף. קשר אותו ב־README של הזיכרון; הוסף מקור, תאריך ומצב אימות. החלטת מוצר נשמרת קודם באפיון/יומן, והזיכרון מפנה אליה.

לפני סיום סשן משמעותי עדכן מצב ויומן, הסר הנחות שכבר התיישנו, ותעד מה טרם נבדק. סמן הצעה ככזאת; אל תכתוב הבטחה או אישור שלא התקבלו. שמור בזיכרון רק מידע פרויקטלי מועיל שניתן לפרסם במאגר: ללא נתוני חיילים, פטורים, מספרים אישיים אמיתיים, פרטי קשר, סיסמאות, אסימונים, קודי שחזור או מפתחות גיבוי.
